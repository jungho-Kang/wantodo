# 처음 시작하는 사람을 위한 안내서

프로그래밍을 한 번도 안 해봤어도 따라 할 수 있도록, 설치부터 실행까지
전부 순서대로 설명합니다. 위에서부터 하나씩 그대로 따라 하시면 됩니다.
(Windows 컴퓨터 기준입니다.)

중간에 뭔가 안 되거나 이해가 안 되는 부분이 있으면, 어느 단계에서
막혔는지랑 화면 스크린샷을 같이 알려주세요.

---

## 1단계. 필요한 프로그램 3개 설치하기

아래 3개를 순서대로 설치합니다. 전부 무료입니다.

### 1-1. Git 설치

Git은 코드를 내려받고 관리하는 프로그램입니다.

1. https://git-scm.com/download/win 접속
2. 자동으로 다운로드가 시작됩니다 (안 되면 "64-bit Git for Windows Setup" 링크를 직접 클릭)
3. 다운로드된 설치 파일을 실행
4. 설치 화면에서 나오는 옵션들은 **전부 기본값 그대로 두고 "Next"만 계속 눌러서** 끝까지 진행 (설치 버튼은 보통 "Install")
5. 설치가 끝나면 "Finish"

> Git을 설치하면 **Git Bash**라는 프로그램도 같이 설치됩니다. 이건
> 나중에 4단계에서 씁니다.

### 1-2. Node.js 설치

Node.js는 이 프로젝트(자바스크립트로 만들어진 앱)를 실행하는 데 필요한
프로그램입니다.

1. https://nodejs.org/ko/download 접속
2. **Windows 설치 프로그램(.msi)** 클릭
3. 다운로드된 설치 파일을 실행
4. 마찬가지로 옵션 전부 기본값 그대로 두고 "Next" → "Install" → "Finish"

### 1-3. VS Code 설치

VS Code는 코드를 보고 수정하고, 명령어도 입력할 수 있는 프로그램(에디터)입니다.

1. https://code.visualstudio.com 접속
2. "Download for Windows" 클릭해서 다운로드
3. 설치 파일 실행
4. 진행하다 보면 체크박스 목록이 나오는데, 아래 항목들은 **체크해두는 걸 추천**합니다 (편의 기능들이라 없어도 되지만 있으면 편함):
   - "Add to PATH"
   - "Register Code as an editor for supported file types"
5. 나머지도 기본값으로 두고 끝까지 설치

---

## 2단계. 코드 받기

### 2-1. GitHub 계정 만들기 (계정이 없다면)

https://github.com 에서 회원가입 (이메일만 있으면 됩니다). 이 저장소는
공개(Public)라서 계정이 없어도 코드를 받는 것 자체는 가능하지만, 나중에
이슈를 남기거나 협업하려면 계정이 있는 게 좋습니다.

### 2-2. 저장소 주소 복사

1. 전달받은 저장소 링크(`https://github.com/...`)로 접속
2. 페이지 오른쪽 위쪽에 있는 초록색 **"Code"** 버튼 클릭
3. "HTTPS" 탭이 선택된 상태에서, 그 아래 나오는 주소를 복사 (`https://github.com/...` 로 시작하는 주소, 옆에 복사 아이콘 있음)

### 2-3. 코드를 내 컴퓨터에 내려받을 폴더 정하기

**중요**: 바탕화면이나 OneDrive/Google Drive 등으로 동기화되는 폴더
안에는 받지 마세요 (나중에 실행할 때 에러가 날 수 있습니다). 예를 들어
`C:\projects` 처럼 새 폴더를 하나 만들어서 그 안에 받는 걸 추천합니다.

파일 탐색기에서 `C:\` 드라이브 안에 `projects`라는 새 폴더를 만드세요.

---

## 3단계. 터미널이 뭔지, VS Code에서 여는 법

### "터미널"이 뭔가요?

지금까지는 마우스로 아이콘/버튼을 눌러서 프로그램을 조작했다면,
**터미널**은 그 대신 **글자로 된 명령어를 직접 타이핑해서** 컴퓨터에게
"이거 해줘"라고 시키는 화면입니다. 검은 화면에 글자만 있는 창이 뜨는데,
무섭게 생겼지만 이 문서에 적힌 명령어를 그대로 복사해서 붙여넣기만 하면
됩니다.

### VS Code로 프로젝트 폴더 열기

1. VS Code 실행
2. 왼쪽 위 메뉴에서 **File(파일) > Open Folder(폴더 열기)** 클릭
3. 방금 만든 `C:\projects` 폴더 선택 (아직 프로젝트를 안 받았다면 이 폴더를 선택하고, 3-1 순서대로 진행)

### 터미널 여는 단축키

VS Code 화면 안에서 키보드로 **`Ctrl` + `` ` `` (백틱, 숫자 1 왼쪽에 있는 키)**
를 누르면 화면 아래쪽에 터미널 창이 나타납니다. 이 단축키를 자주 쓰게
되니 기억해두세요.

### 기본이 PowerShell인데, Git Bash로 바꾸기

VS Code는 기본적으로 **PowerShell**이라는 터미널을 엽니다. 근데 이
프로젝트 문서(README 등)에 나오는 명령어들은 **Git Bash** 기준으로
적혀 있어서, Git Bash로 바꿔서 쓰는 걸 추천합니다.

두 터미널은 화면에 뜨는 첫 줄 모양으로 구분할 수 있습니다:

- PowerShell: `PS C:\projects>` 처럼 시작
- Git Bash: `사용자이름@컴퓨터이름 MINGW64 /c/projects` 처럼 시작

**Git Bash로 바꾸는 법**:

1. 터미널 창 오른쪽 위에 있는 아래 화살표(`⌄` 모양, `+` 버튼 바로 옆)를 클릭
2. 목록에서 **"Git Bash"** 선택
3. 그러면 Git Bash로 된 새 터미널 탭이 열립니다. 이후로는 `+`로 새 터미널을 열 때마다 이 목록에서 Git Bash를 선택해주세요

(매번 선택하기 귀찮다면: 아래 화살표(`⌄`) 클릭 → **"Select Default Profile"** 클릭 → **"Git Bash"** 선택. 그 다음부터는 `+`를 누르면 자동으로 Git Bash가 열립니다.)

---

## 3-1단계. 코드 내려받기 (git clone)

Git Bash 터미널을 연 상태에서, 아래 명령어를 그대로 입력하고 Enter
(단, `<여기에 주소 붙여넣기>` 부분은 2-2단계에서 복사한 실제 주소로 바꿔야 합니다):

```bash
git clone <여기에 주소 붙여넣기>
```

예를 들어 주소가 `https://github.com/jungho-Kang/wantodo.git`
이라면:

```bash
git clone https://github.com/jungho-Kang/wantodo.git
```

공개(Public) 저장소라 로그인 없이 바로 다운로드됩니다.

다운로드가 끝나면, 방금 만든 `C:\projects` 폴더 안에 프로젝트 폴더가
새로 생깁니다.

**그 다음, VS Code에서 이 폴더를 다시 열어주세요**: File > Open Folder

> 방금 새로 생긴 프로젝트 폴더 선택.

---

## 4단계. 프로젝트 설치하기

VS Code로 프로젝트 폴더를 연 상태에서 `Ctrl` + `` ` ``로 터미널을 열고
(Git Bash인지 확인), 아래 명령어를 순서대로 입력합니다.

### 4-1. 필요한 부품들 설치

```bash
npm install
```

시간이 좀 걸립니다 (1~5분 정도). 중간에 빨간 글씨가 몇 줄 나와도
"npm warn" 정도면 괜찮습니다. 끝나면 다시 명령어를 입력할 수 있는
상태로 돌아옵니다.

### 4-2. 실행하기

```bash
npx expo start --tunnel
```

잠시 기다리면 터미널에 **QR 코드**(네모난 격자무늬 이미지)가 나타납니다.

---

## 5단계. 휴대폰에서 확인하기

1. 휴대폰에서 **Expo Go** 앱을 앱스토어(Play 스토어/App Store)에서 설치
2. Expo Go 앱을 열고 **"Scan QR code"** 버튼 누르기
3. 터미널에 떠 있는 QR 코드를 휴대폰 카메라로 스캔
4. 잠시 기다리면 휴대폰에서 앱이 실행됩니다

---

## 문제가 생겼을 때

- 터미널에 에러 메시지가 나오면, **그 메시지를 통째로 복사**해서
  보내주세요 (스크린샷도 좋습니다).
- 자주 나오는 문제들과 해결법은 [README.md](./README.md)의
  "실행 중 자주 겪는 문제" 항목에도 정리되어 있습니다.
- 터미널에 명령어를 입력할 땐 **오타 없이 정확히** 입력해야 합니다.
  이 문서의 명령어는 마우스로 드래그해서 복사한 다음, 터미널에서는
  `Ctrl+V` 대신 마우스 오른쪽 버튼 클릭(또는 `Ctrl+Shift+V`)으로
  붙여넣는 게 안전합니다.
